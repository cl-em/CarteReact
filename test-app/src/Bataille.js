import './App.css';
import 'App.js';

import {
    useNavigate
} from "react-router-dom";



socket.emit("infoLobby", IdJoueur, IdPartie); 

socket.on("demandeBataille", (data) => { //liste de joueurs (liste de json), taille du paquet, liste de cartes, carte avec valeur et couleur comme attribut
    function jeuBataille(){
        const navigate = useNavigate();


        return (
        
        )

    }
});

export const Bataille = ()=>{
    return (<jeuBataille/>);
};